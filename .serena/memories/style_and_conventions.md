Code is written in Kotlin with straightforward service/tool classes and minimal abstraction. Tool implementations extend `BaseMcpTool`; JetBrains MCP integration lives in `tools/adapters`. Keep tool names Serena-compatible. Prefer small, direct changes over wrappers. Tests use `BasePlatformTestCase` through `CodeLensTestBase`. Documentation and changelog are kept in sync with shipped tool surface.