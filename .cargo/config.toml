[build]
# To enable sccache locally, set: CARGO_BUILD_RUSTC_WRAPPER=sccache
# rustc-wrapper = "sccache"

[profile.release]
# Already optimized in workspace Cargo.toml
# lto = true, strip = true, codegen-units = 1, opt-level = 3, panic = "abort"
