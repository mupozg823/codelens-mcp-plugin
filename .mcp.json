{
  "_comment": "codelens-plugin dev uses the dedicated single-writer dev daemon on :7736 (label dev.codelens.mcp-dev-mutation, binary codelens-mcp-http-dev). Rebuild it with scripts/redeploy-dev-daemon.sh; the shared consumption writer on :7838 remains untouched.",
  "mcpServers": {
    "codelens": {
      "type": "http",
      "url": "http://127.0.0.1:7736/mcp",
      "headers": {
        "x-codelens-project": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
